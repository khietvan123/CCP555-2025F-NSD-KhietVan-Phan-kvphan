export { JwtVerifier } from "aws-jwt-verify/jwt-verifier";
export { CognitoJwtVerifier } from "aws-jwt-verify/cognito-verifier";
export { AlbJwtVerifier } from "aws-jwt-verify/alb-verifier";
import { JwtVerifier } from "aws-jwt-verify/jwt-verifier";
/**
 * @deprecated since version 5.0.0, use JwtVerifier instead.
 *   The JwtRsaVerifier has been aliased to JwtVerifier, that supports Elliptic Curve algorithms as well.
 */
export declare const JwtRsaVerifier: typeof JwtVerifier;
